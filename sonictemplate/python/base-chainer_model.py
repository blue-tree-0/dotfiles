import chainer
import chainer.links as L
import chainer.functions as F


class {{_expr_: substitute('{{_input_: name}}', '/w/+', '/u/0', '')}}(chainer.Chain):
    def __init__(self):
        super({{_input_: name}}, self).__init__()

        with self.init_scope():

    def __call__(self, x):

        return h
